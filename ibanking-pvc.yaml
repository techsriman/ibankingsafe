apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ibankingpvc
spec:
  storageClassName: ibankingStorageClass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
